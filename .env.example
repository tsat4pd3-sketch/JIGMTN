# Preferred production storage: REST API backed by SQL (PostgreSQL / MS SQL / MySQL)
VITE_SQL_API_URL=https://pm-jig-api.example.com/api
VITE_SQL_API_KEY=replace-with-api-token

# Legacy/demo fallback when SQL API is not configured
VITE_GITHUB_REPO=tsat4pd3-sketch/JIGMTN
VITE_GITHUB_TOKEN=
